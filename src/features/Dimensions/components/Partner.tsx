import styled from "@emotion/styled";
import { FC } from "react";

const Root = styled('div')(() => ({
  background: '#f33'
}));

export const Partner: FC<{}> = () => {
  return (
    <Root>
    </Root>
  )
}
