import { FC } from 'react';
import styled from '@emotion/styled';

const Root: any = styled('select')(({theme}:any) => ({
}));

export const SelectField: FC<any> = ({ children }) => {
  return (
    <Root>
    </Root>
  );
}
